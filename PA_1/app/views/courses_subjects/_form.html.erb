<%= form_for(courses_subject) do |f| %>
  <% if courses_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(courses_subject.errors.count, "error") %> prohibited this courses_subject from being saved:</h2>

      <ul>
      <% courses_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.number_field :course_id %>
  </div>

  <div class="field">
    <%= f.label :subject_id %>
    <%= f.number_field :subject_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
